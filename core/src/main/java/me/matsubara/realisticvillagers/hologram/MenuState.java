package me.matsubara.realisticvillagers.hologram;

public enum MenuState {
    MAIN, TALK, INTERACTIONS, CUSTOM,
    // Mayor-only screens, reached by interacting with a village's mayor.
    MAYOR, RADAR
}
